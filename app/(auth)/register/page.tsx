import RegisterForm from "./RegisterForm";

export const metadata = {
  title: "Register — Barangay Mulawin",
  description: "Create a Barangay Mulawin resident account.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
